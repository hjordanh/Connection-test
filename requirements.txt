# Pinned runtime dependencies for the container image. The app can still
# auto-install these on a native install (see _pip_install), but in the image
# they are baked in and runtime auto-install is disabled via NO_AUTO_INSTALL.
Flask>=3.1,<4
Werkzeug>=3.1,<4
python-dotenv>=1.0,<2
# Optional features. anthropic powers AI diagnosis; speedtest-cli gives more
# accurate speed measurements than the built-in HTTP fallback.
anthropic>=0.40,<1.0
speedtest-cli>=2.1,<3
